import { Screen } from '@/src/components/mvp/Screen';

export default function ClientSessionsScreen() {
  return (
    <Screen
      title="My Sessions"
      description="Booking and session management shell is planned after core workout loop."
    />
  );
}
